

function produtoEdit({nomeProd}) {
    return (
      <div>
        <div key={index} className='d-block mb-5 d-flex justify-content-center align-items-center rounded' style={{ maxWidth: "1400px", padding: "10px" }}></div>
      </div>
    );
  }
  
  export default produtoEdit;